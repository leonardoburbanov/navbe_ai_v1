[project]
name = "navbe-connectors"
version = "0.1.0"
description = "Connector plugins (Langfuse first)"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["navbe_connectors"]
